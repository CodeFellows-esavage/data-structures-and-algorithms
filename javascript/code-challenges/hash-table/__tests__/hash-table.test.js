'use strict';

describe('Testing Hashtable Class', () => {
  xit('should store a key/value in the data structure', () => {

  });
  xit('should retreive a value based on the key', () => {

  });
  xit('should return null for a key that does not exist', () => {

  });
  xit('should beable to return a list of all unique keys in the table', () => {

  });
  xit('should succesfully handle a collision', () => {

  });
  xit('should successfully retrieve a value from a bucket within a table that has a collision', () => {

  });
  xit('should successfully hash a key to an in-range value', () => {

  });
});
